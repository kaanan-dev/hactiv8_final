import * as types from '../../Utils/Constants/ActionTypes'

const reducer = (state = [], action) => {
    switch (action.type) {
        case types.GET_MOVIES:
            
            break;
        default:
            break;
    }
}